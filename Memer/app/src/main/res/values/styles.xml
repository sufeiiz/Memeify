<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="android:windowSoftInputMode">stateHidden</item>-->
    </style>

    <style name="CamButtons">
        <item name="android:background">#C1E6FF</item>
        <item name="android:textColor">#232447</item>
    </style>

    <style name="VanillaEditText">
        <item name="android:layout_marginLeft">40dp</item>
        <item name="android:layout_marginRight">40dp</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:hint">Enter Text</item>
        <item name="android:inputType">textMultiLine|textNoSuggestions|textCapCharacters</item>
        <item name="android:isScrollContainer">false</item>
        <item name="android:maxLines">2</item>
        <item name="android:maxLength">46</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:textColorHint">#FFF</item>
    </style>

    <style name="DemoEditText">
        <item name="android:layout_marginLeft">30dp</item>
        <item name="android:layout_marginRight">30dp</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:hint">Enter Text</item>
        <item name="android:focusable">true</item>
        <item name="android:isScrollContainer">false</item>
        <item name="android:maxLength">50</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:textColorHint">#FFF</item>
    </style>
</resources>
